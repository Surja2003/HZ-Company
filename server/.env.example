# Server
NODE_ENV=development
PORT=8080

# CORS
CORS_ORIGINS=http://localhost:5173

# Postgres
DATABASE_URL=postgres://user:password@localhost:5432/hzit

# Rate limiting (optional)
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX=20

# Email (Resend)
RESEND_API_KEY=
MAIL_FROM=HZ IT Company <no-reply@hzit.com>
MAIL_TO=leads@hzit.com

# Optional SMTP settings (only needed if you later switch back to SMTP)
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=
# SMTP_PASS=
